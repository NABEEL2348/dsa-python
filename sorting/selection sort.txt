n=int(input("enter the size of the array"))
arr=[]
count={}
for i in range(n):
    arr.append(int(input("enter the number to store in array")))
i=0
j=1
while(i<n):
    j=i+1
    smallest=arr[i]
    index=i
    while(j<n):
        if(arr[j]<smallest):
            smallest=arr[j]
            index=j
        j=j+1
    if(index!=i):
        swap=arr[i]
        arr[i]=arr[index]
        arr[index]=swap
    
    i=i+1
print(arr)



n=int(input("enter the size of the elements"))
arr=[]
for i in range(n):
    arr.append(int(input("enter the number to insert in an array")))
print(arr)
while(n>0):
    j=0
    largest=arr[n-1]
    index=n-1
    while(j<n):
        if(arr[j]>largest):
            largest=arr[j]
            index=j
        j=j+1
    if(index!=n-1):
        swap=arr[index]
        arr[index]=arr[n-1]
        arr[n-1]=swap
    n=n-1
print(arr)
    
    